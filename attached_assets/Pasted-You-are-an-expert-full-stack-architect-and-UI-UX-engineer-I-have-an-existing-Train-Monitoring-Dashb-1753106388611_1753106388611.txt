You are an expert full‑stack architect and UI/UX engineer. I have an existing Train Monitoring Dashboard built with vanilla PHP/JS/CSS and Supabase REST endpoints (`train_insert.php`, `api_supabase.php`, `get_status.php`). I want to **rewire this project** into a modern, elegant, simple, and stylish codebase using **Vite**, **React**, and **TypeScript**, without touching or altering any of the existing API endpoints or business logic in Supabase. Please deliver:

1. **High‑Level Architecture & Tech Stack**  
   - Project scaffold: Vite + React + TypeScript  
   - Styling: Tailwind CSS (or styled‑components) with a minimal‑elegant theme  
   - Data fetching: React Query or SWR for REST calls to `get_status.php` & speed endpoint  
   - Real‑time: Optionally leverage Supabase Realtime for push updates, or fallback polling  
   - Env config: Use `.env` for Supabase URL & anon key

2. **Folder Structure**  
```

/src
/api
getStatus.ts         # wrappers for existing endpoints
postSpeed.ts
/components
Header.tsx
DashboardGrid.tsx
TrainIndicator.tsx
SpeedPanel.tsx
StatusPanel.tsx
/hooks
useStatus.ts         # React Query hooks
/utils
formatDate.ts
enums.ts             # light/colors mapping
App.tsx
main.tsx
vite.config.ts
tailwind.config.js
tsconfig.json

```

3. **Component Design**  
- **`TrainIndicator`**: reusable card showing checkpoint/station light with dynamic color  
- **`SpeedPanel`**: displays latest speed & mode  
- **`StatusPanel`**: summary cards for running, parked trains, route info  
- **Layout**: CSS Grid for dashboard, responsive mobile + desktop

4. **Data Flow & Hooks**  
- **`useStatus`**: fetches `/get_status.php?mode=speed` and `/get_status.php` every X seconds or via Supabase Realtime  
- Map JSON → TypeScript interfaces (e.g. `Status`, `SpeedLog`, `LightState`)
- Update components via React Query’s `useQuery` + `refetchInterval`

5. **Styling & UX**  
- Tailwind utility classes for spacing, typography, color palette  
- Card shadows, rounded corners (`rounded-2xl`), subtle hover/lift animations (Framer Motion)  
- Dark/light mode toggle via context

6. **Deliverables**  
- Example TSX for `TrainIndicator` and `useStatus` hook  
- Tailwind theme config with color variables matching your current palette  
- README with setup (`npm install` / `npm run dev`), env var guidance, and how to integrate existing APIs

Generate the full plan in English, with code skeletons and configuration files. Focus on elegance, simplicity, maintainability, and preserving all current API/business logic unchanged.